[package]
name = "vykar-common"
version = "0.16.0"
edition = "2021"
license = "GPL-3.0-only"

[lints]
workspace = true

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.3", features = ["user"] }
